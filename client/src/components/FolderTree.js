import FileList from "../features/FileList";

import React from "react";

const FolderTree = () => {
  return (
    <div>
      FolderTree
      <FileList></FileList>
    </div>
  );
};

export default FolderTree;
